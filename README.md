# 🔢 Character Counter

A simple **Character Counter** web application built using **HTML, CSS, and JavaScript**.  
It allows users to type a message in a textarea while **tracking characters in real time** and **preventing input beyond a fixed limit**.

---

## 📌 Scenario

The goal of this project is to create a text area where:
- Users can type a message
- The number of characters typed is shown in real time
- The remaining characters allowed are displayed
- Typing is **prevented** once the maximum limit is reached
- A warning message appears when the limit is exceeded

Maximum character limit used in this project: **200 characters**

---

## 🚀 Features

- Textarea input for user messages
- Real-time character counting
- Displays typed and remaining characters (e.g., `150 / 200 characters`)
- Prevents typing beyond the limit
- Warning message when character limit is reached
- Simple and beginner-friendly implementation

---

## 🛠️ Technologies Used

- **HTML** – Page structure
- **CSS** – Basic styling
- **JavaScript** – Character counting logic and input handling


## 📂 Project Structure

